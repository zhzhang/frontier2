### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Article {
  authors: [User]
  id: String
  reviews: [Review]
  title: String
  versions: [ArticleVersion]
}

type ArticleVersion {
  abstract: String
  createdAt: String
  id: String
  ref: String
}

"""
A date string, such as 2007-12-03, compliant with the `full-date` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar Date

type Mutation {
  assignChair(chairId: String!, submissionId: String!): Submission
  assignReviewers(reviewerIds: [String!]!, submissionId: String!): Submission
  createArticle(abstract: String!, authorIds: [String!]!, ref: String!, title: String!, venueId: String): Article
  createOrganization(description: String!, name: String!): Organization
  createReview(articleId: String!, submissionId: String): Review
  createVenue(description: String!, name: String!, organizationId: String!, submissionDeadline: Date!): Venue
  updateReview(body: String!, id: String!, published: Boolean!, rating: Int!): Review
}

type Organization {
  description: String
  id: String
  name: String
  role: Role
  venues: [Venue]
}

type Query {
  article(id: String!): Article
  articles: [Article]
  browseOrganizations(tags: [String]): [Organization]
  organization(id: String!): Organization
  reviewerAssignedSubmissions: [Submission]
  searchUsers(query: String): [User]
  user(userId: String!): User
  venue(id: String!): Venue
  venueSubmissions(venueId: String!): [Submission]
}

type Review {
  author: User
  body: String
  id: String
  published: Boolean
  rating: Int
  reviewNumber: Int
}

enum Role {
  ADMIN
  MEMBER
  NONE
  REVIEWER
}

type Submission {
  article: Article
  articleId: String
  chair: User
  id: String
  requestedReviewers: [User]
  venueId: String
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

type User {
  articles: [Article]
  email: String
  id: String
  name: String
}

type Venue {
  description: String
  id: String
  name: String
  role: String
  submissionDeadline: Date
}
